@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@*@model Employee*@
@model EmployeeCreateViewModel
@*@inject IEmployeeRepository _empRepository*@

@{ ViewBag.Title = "Create Employee";}


    <form enctype="multipart/form-data" asp-controller="home" asp-action="create" method="post" class="mt-3">
        <div class="form-group row">
            <label asp-for="Name" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10">
                <input asp-for="Name" class="form-control" placeholder="Name" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group row">
            <label asp-for="Email" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10">
                <input asp-for="Email" class="form-control" placeholder="Email" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
        </div>
        <!-- by using Enum-->
        <div class="form-group row">
            <label asp-for="Department" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10">
                <select asp-for="Department" class="custom-select mr-sm-2" asp-items="Html.GetEnumSelectList<DeptEnum>()">
                    <option value="">Please Select</option>
                </select>
                <span asp-validation-for="Department" class="text-danger"></span>
            </div>
        </div>


        <!-- by using static items in option tags
    <div>
    <label asp-for="Department">Department</label>
    <select dat-val="true" data-val-required="The department field is required" id="Department" name="Department">
        <option value="0">None</option>
        <option value="2">IT</option>
        <option value="3">HR</option>
        <option value="4">Payroll</option>
        <option value="5">Technician</option>
        <option value="6">Developer</option>
    </select>
    </div>
    -->
        <div class="form-group row">
            <label asp-for="Photo" class="col-sm-2 col-form-label"></label>
            <div class="col-sm-10"> 
                <div class="custom-file">
                    <input multiple asp-for="Photo" class="form-control custom-file-input" />
                    <label class="custom-file-label">Choose File...</label>
                </div>
            </div>
        </div>

        <div asp-validation-summary="All" class="text-danger"></div>


        <div class="form-group row">
            <div class="col-sm-10">
                <button type="submit" class="btn btn-primary">Create</button>
            </div>
        </div>

        @*<div class="form-group row">
            <div class="col-sm-10">
                Hint: The create view uses also a service instance of MockEmployeeRepository
         Total Employees Count = @_empRepository.GetAllEmployees().Count()
            </div>
        </div>*@

        @section Scripts
        {
        <script>
            $(document).ready(function() {
                $('.custom-file-input').on("change", function() {
                    var fileName = $(this).val().split("\\").pop();
                    $(this).next('.custom-file-label').html(fileName);
                    console.log($(this));
                    //jQuery.fn.init [input#Photos.form-control.custom-file-input]
                    /*
                     * files: FileList
0: File {name: "img4.jpg", lastModified: 1533067030000, lastModifiedDate: Tue Jul 31 2018 21:57:10 GMT+0200 (Eastern European Standard Time), webkitRelativePath: "", size: 34160, …}
1: File {name: "img6.jpg", lastModified
                     */
                   /* var fileLabel = $(this).next('.custom-file-label');
                    var files = $(this)[0].files;
                    //multiple image selection
                    if (files.length > 1) {
                        fileLabel.html(files.length + ' files selected');
                    }
                    //single image selection
                    else if (files.length == 1) {
                        fileLabel.html(files[0].name);
                    }
                    */
                });
            });
        </script>
        }

    </form>